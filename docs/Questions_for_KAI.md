What is "   if label.startswith("B-") or label.startswith("I-"):"   ?
Is it always capitalized?

Code:
  start_idx = text.find(sent, current_idx)

  what if there is more than one ? For example something between two dots, or a signature?


